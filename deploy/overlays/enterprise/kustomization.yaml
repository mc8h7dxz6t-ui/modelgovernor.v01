apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: modelgovernor

resources:
  - ../../base
  - egress-istio.yaml

# Enterprise overlay: zero-trust egress allowlist + production base topology.
# Apply only when Istio is installed in the cluster.
