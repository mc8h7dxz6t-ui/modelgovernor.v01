apiVersion: v1
kind: ConfigMap
metadata:
  name: modelgovernor-config
  namespace: modelgovernor
data:
  RECONCILER_INTERVAL_SECONDS: "20"
  RECONCILER_BATCH_SIZE: "200"
  METRICS_ENABLED: "true"
  RESERVE_TTL_SECONDS: "180"
