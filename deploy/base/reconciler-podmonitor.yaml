apiVersion: monitoring.coreos.com/v1
kind: PodMonitor
metadata:
  name: reconciler
  namespace: modelgovernor
spec:
  selector:
    matchLabels:
      app: reconciler
  podMetricsEndpoints:
    - port: http-health
      path: /metrics/prometheus
      interval: 30s
