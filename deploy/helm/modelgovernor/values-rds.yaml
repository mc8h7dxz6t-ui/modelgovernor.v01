# AWS RDS production overlay — managed Postgres with read replica routing
# Usage:
#   helm upgrade --install mg ./deploy/helm/modelgovernor \
#     -f values-production.yaml -f values-enterprise.yaml -f values-rds.yaml \
#     --set postgres.external.host=mg-prod.cluster-xxxx.us-east-1.rds.amazonaws.com \
#     --set postgres.external.readReplica.host=mg-prod.cluster-ro-xxxx.us-east-1.rds.amazonaws.com

postgres:
  enabled: false
  external:
    enabled: true
    host: mg-prod.cluster-xxxx.us-east-1.rds.amazonaws.com
    port: 5432
    sslMode: require
    readReplica:
      enabled: true
      host: mg-prod.cluster-ro-xxxx.us-east-1.rds.amazonaws.com
      port: 5432
    rds:
      enabled: true
      iamAuth: false

pgbouncer:
  enabled: true
  replicas: 3

secrets:
  databaseReadUrl: postgresql+psycopg://postgres:CHANGE_ME@pgbouncer-read:6432/modelgovernor
