ModelGovernor {{ .Chart.AppVersion }} deployed to namespace {{ include "modelgovernor.namespace" . }}.

Endpoints:
  Gateway:    http://gateway.{{ include "modelgovernor.namespace" . }}.svc:8080/governed/dispatch
  Sidecar:    http://sidecar.{{ include "modelgovernor.namespace" . }}.svc:8081/readyz
  Reconciler: http://reconciler.{{ include "modelgovernor.namespace" . }}.svc:8082/healthz
  Metrics:    http://sidecar.{{ include "modelgovernor.namespace" . }}.svc:8081/metrics/prometheus

Secrets:
{{- if .Values.externalSecrets.enabled }}
  ExternalSecret '{{ include "modelgovernor.secretName" . }}' will sync from your secret store.
{{- else if .Values.secrets.create }}
  Dev secret '{{ include "modelgovernor.secretName" . }}' created from values — rotate before production.
{{- else }}
  Ensure secret '{{ include "modelgovernor.secretName" . }}' exists before workloads become Ready.
{{- end }}

GitOps:
  ArgoCD Application examples live in deploy/argocd/
