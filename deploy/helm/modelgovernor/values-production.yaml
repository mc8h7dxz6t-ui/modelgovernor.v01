sidecar:
  replicas: 3
  hpa:
    minReplicas: 3
    maxReplicas: 12

gateway:
  replicas: 2
  providerMode: live
  oidc:
    enabled: true
    issuerUrl: ""
    audience: modelgovernor

reconciler:
  replicas: 2

postgres:
  enabled: false  # use values-rds.yaml for managed Postgres in production

redis:
  enabled: false
  sentinel:
    enabled: true
    masterName: mymaster
    replicas: 3

externalSecrets:
  enabled: true

secrets:
  create: false

config:
  oidcEnabled: "true"
  gatewayOidcEnabled: "true"
  oidcAllowInternalTokenFallback: "false"
  ledgerAnchorS3ObjectLockEnabled: "true"

syntheticProbe:
  enabled: true

enterprise:
  istio:
    enabled: true
