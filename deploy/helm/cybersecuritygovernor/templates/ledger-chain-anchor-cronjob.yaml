{{- if .Values.ledgerChainAnchor.enabled }}
apiVersion: batch/v1
kind: CronJob
metadata:
  name: security-chain-anchor
  namespace: {{ include "cybersecuritygovernor.namespace" . }}
  labels:
    {{- include "cybersecuritygovernor.labels" . | nindent 4 }}
spec:
  schedule: {{ .Values.ledgerChainAnchor.schedule | quote }}
  concurrencyPolicy: Forbid
  jobTemplate:
    spec:
      template:
        spec:
          restartPolicy: OnFailure
          containers:
            - name: anchor
              image: curlimages/curl:8.5.0
              env:
                - name: SIDECAR_INTERNAL_TOKEN
                  valueFrom:
                    secretKeyRef:
                      name: {{ include "cybersecuritygovernor.secretName" . }}
                      key: sidecar-internal-tokens
              command: ["/bin/sh", "-c"]
              args:
                - |
                  set -e
                  TOKEN="${SIDECAR_INTERNAL_TOKEN%%,*}"
                  curl -sf -H "x-internal-token: ${TOKEN}" \
                    -X POST http://sidecar:8121/internal/security/anchor-head
                  echo "ledger chain head anchored"
{{- end }}
