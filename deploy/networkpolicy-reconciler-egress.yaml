apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: reconciler-egress
  namespace: modelgovernor
spec:
  podSelector:
    matchLabels:
      app: reconciler
  policyTypes:
    - Egress
  egress:
    - to:
        - ipBlock:
            cidr: 0.0.0.0/0
      ports:
        - protocol: TCP
          port: 5432
    - to:
        - namespaceSelector: {}
      ports:
        - protocol: UDP
          port: 53
        - protocol: TCP
          port: 53
