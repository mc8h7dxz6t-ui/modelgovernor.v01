# SubledgerSync

Intercompany reconciliation — FX-hash matching with full CCP commit on matched pairs.

## Standalone

```bash
docker compose -f finance-governor/platforms/subledger_sync/docker-compose.standalone.yml up --build
make -C finance-governor subledger-demo
```
