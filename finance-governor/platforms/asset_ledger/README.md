# AssetLedger

Depreciation engine with regulatory table versioning and spine-backed charge commits.

## Standalone

```bash
docker compose -f finance-governor/platforms/asset_ledger/docker-compose.standalone.yml up --build
make -C finance-governor assetledger-demo
```
