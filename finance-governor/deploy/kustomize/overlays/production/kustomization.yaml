apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ../../base

namespace: finance-governor

commonLabels:
  environment: production

# Full workload manifests: helm install -f deploy/helm/finance-governor/values-production.yaml
