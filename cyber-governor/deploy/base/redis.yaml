apiVersion: apps/v1
kind: Deployment
metadata:
  name: cg-redis
  namespace: cybersecuritygovernor
spec:
  replicas: 1
  selector:
    matchLabels:
      app: cg-redis
  template:
    metadata:
      labels:
        app: cg-redis
    spec:
      containers:
        - name: redis
          image: redis:7
          ports:
            - containerPort: 6379
---
apiVersion: v1
kind: Service
metadata:
  name: cg-redis
  namespace: cybersecuritygovernor
spec:
  selector:
    app: cg-redis
  ports:
    - port: 6379
